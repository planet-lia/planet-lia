package com.planet_lia.match_generator.libs.replays;

public enum ParticleEntityAttribute implements Attribute {
    X,
    Y,
    ROTATION_DEG,
    LAYER,
    OPACITY,
    VISIBILITY,
    SCALE,
    ATTACH,
    EFFECT,
    EMIT
}
